<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>View</title>
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-GLhlTQ8iRABdZLl6O3oVMWSktQOp6b7In1Zl3/Jr59b6EGGoI1aFkw7cmDA6j6gD" crossorigin="anonymous">
        <style>

.parent-container {
    display: flex;
    justify-content: center;
    align-items: center;
    max-width: 80%;
    margin-left: 100px;
    }
        </style>
    </head>
    <body>
        
        <div class="parent-container" style="text-align: center; margin-top: 10%;">
           
            <br><br>
        <table class="table table-light table-bordered table-striped">
            <thead>
                <tr>
                    <th>Name</th>
                    <th>Email</th>
                    <th>Action</th>
                </tr>
            </thead>
            <tbody>
                <% for(var i= 0 ; i <data.length; i++) { %>
                <tr>
                    <td><%= data[i].name %></td>
                    <td><%= data[i].email %></td>

                    <td>
                   
                        <form action="/delete" method="post">
                            
                            <input type="hidden" name="id" id="id" value="<%= data[i]._id %>">
                            <button class="btn btn-danger" type="submit">Delete</button>
                        </form>
                        <a class="btn btn-warning" href="/edit/<%= data[i]._id %>">Edit</a>
                    </td>
                </tr>
                <% } %>
            </tbody>
        </table>
        </div>
        <div style="float: right;margin-right: 12%;">
            <a class="btn btn-primary" href="/create">Create</a>
        </div>
    </body>
</html>
